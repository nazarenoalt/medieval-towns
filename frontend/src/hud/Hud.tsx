export default function Hud() {
  return (
    <div style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}>
      {/* HUD elements go here — use pointerEvents: 'auto' on interactive children */}
    </div>
  )
}
