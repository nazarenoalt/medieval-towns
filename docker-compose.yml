services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: medieval
      POSTGRES_PASSWORD: medieval
      POSTGRES_DB: medieval_towns
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
